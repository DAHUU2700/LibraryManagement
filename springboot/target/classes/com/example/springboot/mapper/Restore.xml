<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.mapper.RestoreMapper">

    <!--  还书_查询  -->
    <select id="listCondition" resultType="com.example.springboot.entity.Restore">
        select * from restore
        <where>
            <if test="bookName != null and bookName != ''">
                book_name like concat('%', #{bookName}, '%')
            </if>
            <if test="bookNo != null and bookNo != ''">
                and book_no = #{bookNo}
            </if>
            <if test="userName != null and userName != ''">
                and user_name like concat('%', #{userName}, '%')
            </if>
        </where>
        order by id desc
    </select>

    <!--  还书_插入  -->
    <insert id="save">
        insert into restore(book_name,book_no,user_no,user_name,user_phone,score,days,return_date,real_date,status)
        values (#{bookName},#{bookNo},#{userNo},#{userName},#{userPhone},#{score},#{days},#{returnDate},#{realDate},#{status})
    </insert>

    <!--  还书_删除  -->
    <delete id="deleteById">
        delete from borrow where id = #{id}
    </delete>

    <!--  更新状态  -->
    <update id="updateStatus">
        update borrow
        set status = #{status}
        where id = #{id}
    </update>



</mapper>